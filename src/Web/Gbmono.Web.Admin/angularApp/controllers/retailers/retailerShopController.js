/*
    retailer shop list controller
*/
(function (module) {
    // inject the controller params
    ctrl.$inject = ['$scope'];

    // create controller
    module.controller('retailerShopListController', ctrl);

    // controller body
    function ctrl($scope) {

        // page init
        init();

        function init() {

        }

        function bindShopGrid() {

        }
    }
})(angular.module('gbmono'));