/*
    brand detail controller
*/
(function (module) {
    // inject the controller params
    ctrl.$inject = [];

    // create controller
    module.controller('topicListController', ctrl);

    // controller body
    function ctrl() {
        var vm = this;

        init(); // page init 

        function init() {

        }  
    }
})(angular.module('gbmono'));